BRONCODE EN UITLEG - Transit Planner
====================================

Laatst herzien: 31 mei 2026

Dit project is een Java/Maven/JavaFX-applicatie voor een eenvoudige OV-reisplanner.
De huidige applicatie gebruikt vaste voorbeeldroutes, een keuze tussen bus en trein,
een zoekveld, een routekeuze, een lijst met reistijden, een detailscherm en een
assistentiekeuze.


BELANGRIJKSTE HUIDIGE KENMERKEN
===============================

- De applicatie start via com.example.demo.app.Launcher.
- MainApplication opent een JavaFX-venster van 1024 x 760 pixels.
- app.css wordt geladen vanuit src/main/resources/com/example/demo/view/app.css.
- DashboardView bouwt het hoofdscherm en laadt trajectory-selection-view.fxml.
- TrajectorySelectionController bestuurt zoeken, filteren, wisselen en details tonen.
- FixedRoutes bevat vaste routes voor Bus en Trein.
- Trajectory bevat vertrek, aankomst, vertrektijden, reistijd en vervoerstype.
- TransportType is een enum met BUS en TREIN.
- TransportTypeBox toont Bus en Trein als toggle-knoppen met iconen.
- AssistanceToggle vraagt of de gebruiker assistentie tijdens de reis wil.
- De oude HelloApplication en hello-view.fxml staan nog in het project, maar horen
  niet bij de normale startflow.


PROJECTSTRUCTUUR
================

Belangrijkste bestanden:

- pom.xml
  Maven-configuratie met JavaFX- en JUnit-dependencies.

- README.md
  Korte projectbeschrijving met startinstructies.

- src/main/java/com/example/demo/app/Launcher.java
  Startpunt met de main-methode.

- src/main/java/com/example/demo/app/MainApplication.java
  JavaFX Application-klasse die het hoofdvenster opent en CSS laadt.

- src/main/java/com/example/demo/app/AppFactory.java
  Tussenlaag die trajectdata uit FixedRoutes beschikbaar maakt.

- src/main/java/com/example/demo/controller/TrajectorySelectionController.java
  Controller voor zoeken, routefiltering, wisselen, reistijden en reisdetails.

- src/main/java/com/example/demo/model/FixedRoutes.java
  Vaste voorbeeldroutes voor bus en trein.

- src/main/java/com/example/demo/model/Trajectory.java
  Modelklasse voor een route.

- src/main/java/com/example/demo/model/TransportType.java
  Enum voor BUS en TREIN.

- src/main/java/com/example/demo/view/AssistanceToggle.java
  UI-component voor assistentie ja/nee.

- src/main/java/com/example/demo/view/DashboardView.java
  Bouwt de hoofdcontainer en laadt de FXML.

- src/main/java/com/example/demo/view/SelectionButton.java
  Herbruikbare toggle-knop voor de vervoerstypekeuze.

- src/main/java/com/example/demo/view/TransportTypeBox.java
  UI-component met Bus- en Trein-knoppen.

- src/main/resources/com/example/demo/view/trajectory-selection-view.fxml
  Hoofd-FXML voor de reisplanner.

- src/main/resources/com/example/demo/view/app.css
  Styling voor de applicatie.


HOE START DE APPLICATIE?
========================

De normale startflow is:

1. Launcher.main() wordt uitgevoerd.
2. Launcher start JavaFX met MainApplication:

   Application.launch(MainApplication.class, args);

3. JavaFX roept MainApplication.start(Stage stage) aan.
4. MainApplication maakt een DashboardView.
5. MainApplication maakt een Scene van 1024 x 760 pixels.
6. MainApplication laadt app.css.
7. DashboardView maakt een BorderPane en plaatst daarin de FXML-weergave.
8. trajectory-selection-view.fxml gebruikt TrajectorySelectionController.
9. Het venster krijgt de titel "Reis Planner".


WAT DOET DE APPLICATIE?
=======================

1. De gebruiker ziet de titel "OV-reisplanner".
2. De gebruiker kiest Bus of Trein. Bus is standaard geselecteerd.
3. Het zoekveld filtert routes op vertrek, aankomst of volledige routenaam.
4. De ComboBox toont alleen routes van het gekozen vervoerstype.
5. De knop "Wissel" zoekt de omgekeerde route met hetzelfde vervoerstype.
6. De lijst "Reistijden" toont vertrek, aankomst en duur.
7. Als de gebruiker een reistijd aanklikt, wordt een detailscherm geopend.
8. Het detailscherm toont vertrekstation, aankomststation, vertrektijd,
   aankomsttijd, reisduur, vervoerstype en alle beschikbare tijden.
9. De knop "Terug" brengt de gebruiker terug naar de zoekweergave.
10. Onderaan kan de gebruiker aangeven of assistentie nodig is.


BESTAND: pom.xml
================

Uitleg:
Dit is het Maven-configuratiebestand. Het project gebruikt JavaFX controls,
JavaFX FXML en JUnit. De compiler staat op Java 21.

Belangrijke huidige instellingen:
- Java compiler source/target/release: 21
- JavaFX-versie: 21.0.6
- JUnit-versie: 5.12.1
- JavaFX Maven mainClass: com.example.demo/com.example.demo.app.Launcher


BESTAND: src/main/java/com/example/demo/app/Launcher.java
=========================================================

Uitleg:
Launcher bevat de main-methode en start JavaFX met MainApplication.

Broncode:

package com.example.demo.app;

import javafx.application.Application;

public class Launcher {
    public static void main(String[] args) {
        Application.launch(MainApplication.class, args);
    }
}


BESTAND: src/main/java/com/example/demo/app/MainApplication.java
===============================================================

Uitleg:
MainApplication is de hoofdklasse van de JavaFX-applicatie. Deze klasse maakt
DashboardView, zet die in een Scene, laadt app.css en opent het venster.

Broncode:

package com.example.demo.app;

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.stage.Stage;
import com.example.demo.view.DashboardView;

import java.util.Objects;

public class MainApplication extends Application {

    @Override
    public void start(Stage stage) {
        DashboardView startScreen = new DashboardView();

        Scene scene = new Scene(startScreen.getRoot(), 1024, 760);
        scene.getStylesheets().add(Objects.requireNonNull(
                MainApplication.class.getResource("/com/example/demo/view/app.css")
        ).toExternalForm());
        stage.setTitle("Reis Planner");
        stage.setScene(scene);
        stage.show();
    }
}


BESTAND: src/main/java/com/example/demo/app/AppFactory.java
==========================================================

Uitleg:
AppFactory is een eenvoudige tussenlaag. De controller vraagt trajecten op via
AppFactory.getTrajectory(), waarna AppFactory de data uit FixedRoutes haalt.

Broncode:

package com.example.demo.app;
import java.util.List;

import com.example.demo.model.FixedRoutes;
import com.example.demo.model.Trajectory;

public class AppFactory {


        public static List<Trajectory> getTrajectory() {
            return FixedRoutes.getAllRoutes();

        }
    }


BESTAND: src/main/java/com/example/demo/model/TransportType.java
===============================================================

Uitleg:
TransportType is een enum voor de beschikbare vervoerstypen. De labels worden
getoond in de UI.

Broncode:

package com.example.demo.model;

public enum TransportType {
    BUS("Bus"),
    TREIN("Trein"); // choose displayed label here

    private final String label;

    TransportType(String label) {
        this.label = label;
    }

    public String getLabel() {
        return label;
    }

    @Override
    public String toString() {
        return label;
    }
}


BESTAND: src/main/java/com/example/demo/model/Trajectory.java
============================================================

Uitleg:
Trajectory is de modelklasse voor een traject.

Velden:
- departure: vertrekplaats.
- arrival: aankomstplaats.
- departureTimes: beschikbare vertrektijden.
- travelMinutes: reistijd in minuten.
- transportType: Bus of Trein.

Belangrijke methodes:
- getDurationString() zet minuten om naar leesbare tekst.
- toString() bepaalt de routeweergave in de ComboBox.

Broncode:

package com.example.demo.model;

import java.util.List;

public class Trajectory {
    private static final String ARROW = " → ";

    private final String departure;
    private final String arrival;
    private final List<String> departureTimes;
    private final int travelMinutes;
    private final TransportType transportType;

    public Trajectory(String departure, String arrival, List<String> departureTimes, int travelMinutes, TransportType transportType) {
        this.departure = departure;
        this.arrival = arrival;
        this.departureTimes = departureTimes;
        this.travelMinutes = travelMinutes;
        this.transportType = transportType;
    }

    public TransportType getTransportType(){
        return transportType;
    }

    public String getDeparture() {
        return departure;
    }

    public String getArrival() {
        return arrival;
    }

    public List<String> getDepartureTimes() {
        return departureTimes;
    }

    public int getTravelMinutes() {
        return travelMinutes;
    }

    public String getDurationString() {
        int hours = travelMinutes / 60;
        int minutes = travelMinutes % 60;

        if (hours == 0) {
            // Alleen minuten
            return minutes + " minuten";
        } else if (minutes == 0) {
            // Alleen uren
            return hours + " uur";
        } else {
            // Beide uren en minuten
            return hours + " uur " + minutes + " minuten";
        }
    }

    @Override
    public String toString() {
        return departure + ARROW + arrival;
    }
}


BESTAND: src/main/java/com/example/demo/model/FixedRoutes.java
=============================================================

Uitleg:
FixedRoutes bevat vaste voorbeelddata. Voor elke hoofdroute bestaat er een
heen- en terugrichting. Er zijn routes voor Bus en Trein.

Routes:
- Zwolle <-> Utrecht
- Amsterdam <-> Rotterdam
- Groningen <-> Leeuwarden

Broncode:

package com.example.demo.model;

import java.util.List;

public class FixedRoutes {

    public static List<Trajectory> getAllRoutes() {
        return List.of(
                //Bus routes
                new Trajectory("Zwolle", "Utrecht", List.of("08:30", "10:15", "17:35"), 55, TransportType.BUS),
                new Trajectory("Utrecht", "Zwolle", List.of("09:05", "12:05", "18:10"), 55, TransportType.BUS),
                //Trein routes
                new Trajectory("Zwolle", "Utrecht", List.of("07:00", "11:30", "16:45"), 45, TransportType.TREIN),
                new Trajectory("Utrecht", "Zwolle", List.of("08:15", "13:00", "17:20"), 45, TransportType.TREIN),

                //Bus
                new Trajectory("Amsterdam", "Rotterdam", List.of("07:05", "12:40", "18:20"), 95, TransportType.BUS),
                new Trajectory("Rotterdam", "Amsterdam", List.of("08:10", "14:05", "19:40"), 95, TransportType.BUS),
                //Trein
                new Trajectory("Amsterdam", "Rotterdam", List.of("07:45", "13:00", "19:00"), 50, TransportType.TREIN),
                new Trajectory("Rotterdam", "Amsterdam", List.of("09:00", "11:15", "20:00"), 50, TransportType.TREIN),

                //Bus
                new Trajectory("Groningen", "Leeuwarden", List.of("09:10", "13:55", "20:05"), 100,TransportType.BUS),
                new Trajectory("Leeuwarden", "Groningen", List.of("07:35", "15:10", "21:15"), 100, TransportType.BUS),

                //trein
                new Trajectory("Groningen", "Leeuwarden", List.of("08:00", "13:00", "19:45"), 85,TransportType.TREIN),
                new Trajectory("Leeuwarden", "Groningen", List.of("09:35", "14:00", "21:40"), 85,TransportType.TREIN)

        );
    }
}


BESTAND: src/main/java/com/example/demo/view/DashboardView.java
==============================================================

Uitleg:
DashboardView bouwt het hoofdscherm. De root is een BorderPane met CSS-class
app-root. In het midden staat een VBox met CSS-class planner-shell. Daarin wordt
trajectory-selection-view.fxml geladen.

Broncode:

package com.example.demo.view;

import javafx.fxml.FXMLLoader;
import javafx.geometry.Insets;
import javafx.scene.Parent;
import javafx.scene.layout.BorderPane;
import javafx.scene.layout.VBox;

import java.io.IOException;

public class DashboardView {
    private final BorderPane mainLayout;

    public DashboardView() {
        this.mainLayout = new BorderPane();
        this.mainLayout.setPadding(new Insets(32));
        this.mainLayout.getStyleClass().add("app-root");

        VBox centerContent = new VBox();
        centerContent.setSpacing(20);
        centerContent.getStyleClass().add("planner-shell");

        centerContent.getChildren().add(loadTrajectorySelection());

        mainLayout.setCenter(centerContent);
    }

    private Parent loadTrajectorySelection() {
        try {
            return FXMLLoader.load(DashboardView.class.getResource("/com/example/demo/view/trajectory-selection-view.fxml"));
        } catch (IOException e) {
            throw new IllegalStateException("Het laden van de trajectselectieweergave is mislukt.", e);
        }
    }

    public BorderPane getRoot() {
        return mainLayout;
    }
}


BESTAND: src/main/java/com/example/demo/view/SelectionButton.java
================================================================

Uitleg:
SelectionButton is een herbruikbare ToggleButton. De knop kan met of zonder icoon
worden gemaakt. Bij een icoon wordt tekst boven/onder het icoon gecentreerd.

Broncode:

package com.example.demo.view;

import javafx.geometry.Pos;
import javafx.scene.Node;
import javafx.scene.control.ContentDisplay;
import javafx.scene.control.ToggleButton;
import javafx.scene.control.ToggleGroup;

public class SelectionButton extends ToggleButton {

    public SelectionButton(String label, ToggleGroup group) {
        super(label);
        configure(group);
    }

    public SelectionButton(String label, ToggleGroup group, Node icon) {
        super(label, icon);
        configure(group);
        this.setContentDisplay(ContentDisplay.TOP);
        this.setGraphicTextGap(6);
        this.setAlignment(Pos.CENTER);
    }

    private void configure(ToggleGroup group) {
        this.setToggleGroup(group);

        this.setMinWidth(130);
        this.setMinHeight(84);
        this.setPrefHeight(84);
        this.setPrefWidth(140);
        this.setAlignment(Pos.CENTER);

        this.getStyleClass().add("transport-toggle");
    }
}


BESTAND: src/main/java/com/example/demo/view/TransportTypeBox.java
=================================================================

Uitleg:
TransportTypeBox toont twee SelectionButtons: Bus en Trein. Beide zitten in
dezelfde ToggleGroup. Bus is standaard geselecteerd. De knoppen krijgen iconen uit
src/main/resources/icons.

Belangrijke methodes:
- getSelectedType() geeft het geselecteerde TransportType terug.
- setOnTransportTypeChanged(...) laat de controller reageren op wijzigingen.

Broncode:

package com.example.demo.view;

import com.example.demo.model.TransportType;
import javafx.geometry.Pos;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import javafx.scene.control.ToggleGroup;
import javafx.scene.layout.HBox;

import java.util.Objects;

public class TransportTypeBox extends HBox {
    private final ToggleGroup transportTypeGroup;

    public TransportTypeBox() {
        this.transportTypeGroup = new ToggleGroup();

        SelectionButton busButton = new SelectionButton(
                "Bus",
                transportTypeGroup,
                createIcon("/icons/bus icon green.png")
        );
        busButton.setUserData(TransportType.BUS);

        SelectionButton trainButton = new SelectionButton(
                "Trein",
                transportTypeGroup,
                createIcon("/icons/train icon yellow.png")
        );
        trainButton.setUserData(TransportType.TREIN);

        busButton.setSelected(true);

        transportTypeGroup.selectedToggleProperty().addListener((obs, oldVal, newVal) -> {
            if (newVal == null) {
                transportTypeGroup.selectToggle(oldVal);
            }
        });

        this.getChildren().addAll(busButton, trainButton);
        this.setAlignment(Pos.CENTER_LEFT);
        this.setSpacing(10);
        this.getStyleClass().add("transport-type-box");
    }

    private ImageView createIcon(String path) {
        Image image = new Image(
                Objects.requireNonNull(getClass().getResource(path)).toExternalForm()
        );

        ImageView icon = new ImageView(image);
        icon.setFitWidth(40);
        icon.setFitHeight(40);
        icon.setPreserveRatio(true);

        return icon;
    }

    public TransportType getSelectedType() {
        SelectionButton chosenButton = (SelectionButton) transportTypeGroup.getSelectedToggle();
        return (chosenButton != null) ? (TransportType) chosenButton.getUserData() : null;
    }

    public void setOnTransportTypeChanged(java.util.function.Consumer<TransportType> callback) {
        transportTypeGroup.selectedToggleProperty().addListener((obs, oldVal, newVal) -> {
            if (newVal != null) {
                callback.accept(getSelectedType());
            }
        });
    }
}


BESTAND: src/main/java/com/example/demo/view/AssistanceToggle.java
=================================================================

Uitleg:
AssistanceToggle toont de vraag of de gebruiker assistentie wil. De keuze bestaat
uit Ja/Nee. Nee is standaard geselecteerd.

Broncode:

package com.example.demo.view;

import javafx.scene.control.Label;
import javafx.geometry.Insets;
import javafx.scene.control.ToggleButton;
import javafx.scene.control.ToggleGroup;
import javafx.scene.layout.HBox;
import javafx.scene.layout.VBox;


public class AssistanceToggle extends VBox {

    private final ToggleButton yesButton;
    private final ToggleButton noButton;
    private final Label statusLabel;


    public AssistanceToggle() {
        setSpacing(10);
        setPadding(new Insets(12, 0, 0, 0));
        getStyleClass().add("assistance-section");

        Label title = new Label("Wilt u assistentie tijdens uw reis?");
        title.getStyleClass().add("field-label");

        ToggleGroup group = new ToggleGroup();
        yesButton = new ToggleButton("Ja");
        noButton = new ToggleButton("Nee");
        yesButton.setToggleGroup(group);
        noButton.setToggleGroup(group);
        noButton.setSelected(true);

        yesButton.getStyleClass().add("choice-toggle");
        noButton.getStyleClass().add("choice-toggle");

        statusLabel = new Label("U reist zelfstandig.");
        statusLabel.getStyleClass().add("message-label");

        group.selectedToggleProperty().addListener((obs, oldVal, newVal) -> {
            if (newVal == yesButton) {
                statusLabel.setText("U reist met assistentie.");
            } else {
                noButton.setSelected(true);
                statusLabel.setText("U reist zelfstandig.");
            }
        });

        HBox choices = new HBox(8, yesButton, noButton, statusLabel);
        choices.getStyleClass().add("assistance-choices");

        getChildren().addAll(title, choices);
    }

    public boolean isAssistanceEnabled() {
        return yesButton.isSelected();
    }
}


BESTAND: src/main/java/com/example/demo/controller/TrajectorySelectionController.java
==================================================================================

Uitleg:
TrajectorySelectionController bestuurt trajectory-selection-view.fxml.

Belangrijke onderdelen:
- routeBox: ComboBox met gefilterde Trajectory-objecten.
- searchField: los tekstveld voor zoeken.
- transportTypeBox: keuze tussen Bus en Trein.
- swapButton: knop om de omgekeerde route te kiezen.
- timesList: lijst met beschikbare reistijden.
- searchPane: zoek- en tijdenweergave.
- detailsScrollPane/detailsPane: detailweergave na selectie van een reistijd.
- backButton: terug naar de zoekweergave.
- allRoutes: volledige routelijst.
- selectedTrajectory en selectedDepartureTime: huidige keuze voor het detailscherm.

Belangrijke logica:
- initialize() zet listeners en toont de zoekweergave.
- applyFilters() filtert op zoektekst en vervoerstype.
- onSwap() zoekt een omgekeerde route met hetzelfde vervoerstype.
- refreshTimes() berekent aankomsttijden met LocalTime.plusMinutes().
- onTravelSelected() opent het detailscherm.
- updateDetailsDisplay() vult de detailvelden.
- onBack() gaat terug naar de zoekweergave.


BESTAND: src/main/resources/com/example/demo/view/trajectory-selection-view.fxml
==============================================================================

Uitleg:
Dit FXML-bestand beschrijft de hoofdweergave van de reisplanner:
- titel "OV-reisplanner";
- formulier "Plan je reis";
- TransportTypeBox voor Bus/Trein;
- TextField voor zoeken;
- ComboBox voor routekeuze;
- Wissel-knop;
- ListView voor reistijden;
- ScrollPane met reisdetails;
- AssistanceToggle onderaan.

Let op:
De huidige FXML bevat tekst met pijlsymbolen. Als deze verkeerd wordt weergegeven
als mojibake, controleer dan de bestandsencoding in de editor en gebruik UTF-8.


BESTAND: src/main/resources/com/example/demo/view/app.css
=========================================================

Uitleg:
app.css bevat de styling voor:
- de achtergrond van de applicatie;
- de centrale plannerkaart;
- titels, labels en meldingen;
- zoekveld en route-ComboBox;
- primaire knoppen;
- bus/trein-toggleknoppen;
- reistijdenlijst;
- detailpaneel;
- assistentieblok.


BESTAND: src/main/java/com/example/demo/view/HelloApplication.java
=================================================================

Uitleg:
HelloApplication is een overgebleven JavaFX-templateklasse. De normale applicatie
start niet via deze klasse, maar via com.example.demo.app.Launcher.

Broncode:

package com.example.demo.view;

import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.scene.Scene;
import javafx.stage.Stage;

import java.io.IOException;

public class HelloApplication extends Application {
    @Override
    public void start(Stage stage) throws IOException {
        FXMLLoader fxmlLoader = new FXMLLoader(HelloApplication.class.getResource("hello-view.fxml"));
        Scene scene = new Scene(fxmlLoader.load(), 320, 240);
        stage.setTitle("Hallo!");
        stage.setScene(scene);
        stage.show();
    }
}


BESTAND: src/main/resources/com/example/demo/view/hello-view.fxml
=================================================================

Uitleg:
hello-view.fxml hoort bij de oude templateweergave. Het bestand verwijst naar
com.example.demo.controller.HelloController. In de huidige src-map staat die
controller niet meer als bronbestand. Dit is geen probleem voor de normale
startflow, omdat Launcher/MainApplication deze hello-view niet gebruikt.


SAMENVATTING VAN DE HUIDIGE WERKING
===================================

Start:
Launcher -> MainApplication -> DashboardView -> trajectory-selection-view.fxml

Controller:
TrajectorySelectionController haalt routes op via AppFactory, filtert deze op
TransportType en zoektekst, toont vertrektijden en opent een detailscherm wanneer
een tijd wordt geselecteerd.

Data:
TrajectorySelectionController -> AppFactory -> FixedRoutes -> Trajectory-objecten.

UI:
TransportTypeBox bepaalt Bus/Trein, AssistanceToggle toont de assistentiekeuze,
app.css bepaalt de vormgeving.


CONTROLE
========

Controle op 31 mei 2026:
- broncode_uitleg.txt is bijgewerkt op basis van de huidige bestanden in src.
- pom.xml gebruikt Java compiler source/target/release 21.
- De huidige app gebruikt een apart zoekveld in plaats van een bewerkbare ComboBox.
- De Bus/Trein-keuze wordt nu gebruikt om routes te filteren.
- De applicatie heeft een detailscherm voor geselecteerde reistijden.
- De applicatie heeft een assistentie-toggle.
- De broncode zelf is bij deze documentatie-update niet aangepast.
- mvn test is niet uitgevoerd, omdat dit alleen een tekstupdate is.


ADVIEZEN
========

1. Werk AppFactory.java netjes op qua inspringing en imports.
2. Controleer de encoding van pijlsymbolen in Trajectory.java en FXML-bestanden;
   gebruik bij voorkeur overal UTF-8 of vervang pijlen door ASCII "->".
3. Verwijder HelloApplication en hello-view.fxml, of voeg HelloController terug
   toe als deze template bewust bewaard moet blijven.
4. Overweeg tests toe te voegen voor filteren op TransportType en voor de
   reistijdberekening.
5. Maak de naamgeving consistenter: het project gebruikt Engels in packages en
   modelvelden, maar Nederlands in de UI.
